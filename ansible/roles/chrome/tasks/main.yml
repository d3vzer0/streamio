- name: Run Chrome container
  docker_container:
    image: selenium/node-chrome:latest
    state: started
    recreate: yes
    name: chrome
    env:
      HUB_HOST: "hub"
      HUB_PORT: "4444"
    networks:
      - name: backend_network
